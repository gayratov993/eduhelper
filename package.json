{
  "name": "eduhelper-api",
  "version": "1.0.0",
  "type": "module",
  "description": "EduHelper — kunlik o'quv vazifalari planeri (API + MongoDB Atlas)",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "dev:client": "npm run dev --prefix client",
    "build": "npm run build --prefix client"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "dotenv": "^16.4.7",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^8.19.0"
  }
}